1. `undefined`, потому что переменную `a` надо было создавать до условия `if`, тогда алерт выведет 1.

2.  переменная `a` не определена. 

3. выведется сама функция. Здесь `x` не задано, чтобы что-то в функции считать, функция `a(x)`  не вызывается. Создана переменная `а`, но не задано никакого значения, если в итоге хотели просто значение этой переменной в алерте вывести. 

4. выведется в алерт `10`, потому что задан второй элемент массива равный `10`. Его в алерт и пытаются вывести. Если бы его не задавали внутри функции, то вывелась бы цифра `3` из `b(1,2,3)`.

5.  объект `window`, потому что `this` это текущий объект. В методе `call` он и вызовется. 